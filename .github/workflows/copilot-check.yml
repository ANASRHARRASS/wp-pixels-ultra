name: "copilot"

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

jobs:
  ok:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Quick success
        run: echo "copilot check placeholder — success"